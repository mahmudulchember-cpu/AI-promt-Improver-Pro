
[build]
  # Standard Vite build command
  command = "npm run build"
  # Vite outputs the production site to the 'dist' folder
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
